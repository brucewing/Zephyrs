<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="activity_login.onefriend">
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"/>
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ResultPage"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".BottomBar"
            android:theme="@style/AppTheme.NoActionBar">

        </activity>
        <activity android:name=".lvWithButtonExt"
            android:theme="@style/AppTheme.NoActionBar">

        </activity>
        <activity android:name=".MusicPlay"
            android:theme="@style/AppTheme.NoActionBar">

        </activity>
        <activity android:name=".LoginActivity"
            android:theme="@style/AppTheme.NoActionBar">

        </activity>

        <!--<activity android:name=".InformationPage"-->
            <!--android:theme="@style/AppTheme.NoActionBar">-->

        <!--</activity>-->


        <activity android:name=".HomePageActivity"
            android:theme="@style/AppTheme.NoActionBar">

        </activity>
        <service android:name="activity_login.onefriend.MusicService"></service>
    </application>

</manifest>